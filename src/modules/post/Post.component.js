import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Post extends Component {
  render() {
    return (
      <div className="knc-post-module">
        test
      </div>
    );
  }
}

Post.propTypes = {
};

Post.defaultProps = {
};

export default Post;
